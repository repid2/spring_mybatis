<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 매퍼로 사용될 인터페이스를 namespace로 작성 -->
<mapper
	namespace="com.joonbread.springmybatis.member.repository.MemberDAO">

	<!-- 회원 리스트 -->
	<select id="memberList" resultType="memberDto">
		select *
		from member
		order by user_updateDate
	</select>
	
	<!-- 로그인 -->
	<select id="checkPw" resultType="int">
		select count(*)
		from member
		where
		user_id = #{user_id} and user_pw = #{user_pw}
	</select>

	<!-- 전체 회원 수 -->
	<select id="memberCnt" resultType="int">
		<![CDATA[
			SELECT si.rows
			FROM INFORMATION_SCHEMA.TABLES t
			INNER JOIN sysindexes si on (OBJECT_ID(MEMBER) =
			si.id and si.indid < 2)
			WHERE t.table_name = MEMBER
		]]>
	</select>

	<!-- 회원가입 -->
	<insert id="insertMember">
		insert into member
		values(#{user_id}, #{user_pw}, #{user_name}, #{user_email}
		, #{user_phone} ,
		#{user_gender}, #{user_regDate}, #{user_updateDate} )
	</insert>

	<!-- 회원정보 확인 -->
	<select id="viewMember" resultType="memberDto">
		select *
		from member
		where
		user_id = #{user_id}
	</select>

	<!-- 회원 정보 수정 -->
	<update id="updateMember">
		update member set
		user_pw = #{user_pw},
		user_email = #{user_email},
		user_phone = #{user_phone},
		user_updateDate =
		${user_updateDate}
		where user_id = ${user_id}
	</update>

	<delete id="deleteMember">
		delete from member
		where user_id = #{user_id}
	</delete>


</mapper>